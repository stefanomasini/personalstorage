# Personal Storage

## CLI Tool

`cli/storage-cli` manages custom metadata on Dropbox folders/files.

Commands:

- `storage-cli set <path>` — set metadata (`--usage <text>`, `--ignore`, `--no-ignore`)
- `storage-cli get <path>` — read metadata for a path
- `storage-cli -h` — display help information
